# README Fix — not part of Sprint 14

This was flagged separately during the initial repo analysis (before Sprint
14 was scoped) as stale — the root README still described "Day 1" status
while the repo had already reached Sprint 13. It was deliberately excluded
from Sprint14.zip because Sprint 14's task list ("Implement Sprint 14 only")
covered admin activation, not documentation.

## What changed vs. the original
Only the **Status** and **Roadmap** sections were rewritten to reflect the
real, verified state of the repo (Day 1–4, Sprints 05–13, plus this
project's Sprint 14 admin activation). A note was added to the **Folder
Structure** and **Getting Started** sections pointing at `docs/` and `/admin`
rather than inventing an unverified up-to-date file tree.

## What was kept as-is
Tech Stack, the original Day-1 folder tree (with a disclaimer added, not
replaced), Prerequisites, the shadcn/ui section, and License — none of
these needed correction.

## Not included here
Sprint 15 (Book admin UI) and Sprint 16 (Category/Subcategory backend) were
mentioned as *recommendations* for future work, not implemented code — they
are intentionally not packaged, since generating that code wasn't
requested and would be new work, not "already completed work" to package.
