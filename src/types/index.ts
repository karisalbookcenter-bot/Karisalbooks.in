// Shared TypeScript types will live here as the domain model grows
// (e.g. Book, Category, CartItem, Order). Intentionally empty on Day 1 —
// no database tables or entities have been defined yet.

export {};
